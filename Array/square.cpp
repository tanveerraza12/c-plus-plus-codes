#include <iostream>
#include<cmath>
using namespace std;

int main()
{
    int num;
    cout << "enter no. of array size: ";
    cin >> num;
    int arr[num];
    for (int i = 0; i <= num; i++)
    {
        arr[num] = sqrt(num);
    }
    cout<<num<<" ";
}

// not complete wrong algo